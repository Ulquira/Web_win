<<<<<<< HEAD
# ETAPA 1: Construcción
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
# Instalar TODAS las dependencias (necesitamos TypeScript para compilar)
RUN npm ci

COPY . .
# Compilar el backend a JS puro en la carpeta dist/
RUN npm run build:server

# ETAPA 2: Producción (Ultra ligera)
FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
# Instalar solo dependencias de producción (omite typescript, vite, tailwind, etc.)
RUN npm ci --omit=dev

# Copiar solo el código compilado desde la etapa de construcción
COPY --from=builder /app/dist ./dist
=======
# Stage 1: Dependencias
FROM node:20-alpine AS deps
WORKDIR /app
COPY package*.json ./
# Instalamos TODO (incluyendo devDependencies como tsx)
RUN npm ci

# Stage 2: Producción
FROM node:20-alpine AS runner
WORKDIR /app

# Copiamos package.json
COPY package*.json ./
# Instalamos SOLO producción, excluyendo dependencias pesadas como playwright
RUN npm ci --omit=dev && npm install tsx --no-save

# Copiamos solo el backend
COPY ./server ./server
COPY ./capa_intermedia ./capa_intermedia
>>>>>>> c2ceac1216aeb59bbc7711474b2d68c3ef73b270

EXPOSE 3001

# Ejecutar Node directamente sobre el JS compilado (consume menos RAM y CPU)
CMD ["node", "dist/server/index.js"]